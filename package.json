{
  "name": "notes-backend",
  "version": "1.0.0",
  "description": "Backend for notes application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "migrate": "node -e \"require('dotenv').config(); const db = require('./src/config/database'); console.log('Migration complete');\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "mysql2": "^3.3.3",
    "helmet": "^7.0.0",
    "express-rate-limit": "^6.9.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "engines": {
    "node": ">=16.0.0"
  }

}

